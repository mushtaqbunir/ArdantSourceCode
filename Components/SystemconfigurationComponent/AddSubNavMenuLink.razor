@if (IsVisible)
{
    <FormLoader Istrue="@IsloaderShow"></FormLoader>
    <style>
        .FileNameclass {
            text-overflow: ellipsis;
            width: 174px;
            white-space: nowrap;
            overflow: hidden;
        }
    </style>
    <div id="kt_quick_cart3" class="offcanvas offcanvas-right p-10 offcanvas-on w-25">
        <!--begin::Header-->
        <div class="offcanvas-header d-flex align-items-center justify-content-between pb-7 w-25 popup_header" id="sm-width" kt-hidden-height="46">

            @if (MenuItemId == 0)
            {
                <h4 class="font-weight-bold"> Add New Nav link </h4>
            }
            else
            {
                <h4 class="font-weight-bold"> Update Nav link </h4>
            }

            <a @onclick="CloseSideBar" class="btn" id="kt_quick_cart_close">
                <i class="p-0 fa fa-window-close"></i>
            </a>
        </div>
        <div class="offcanvas-content pt-3">

            <div class="offcanvas-wrapper  scroll-pull scroll ps ps--active-y" style="height: auto;overflow:visible">
                <div class=" ">
                    <EditForm Model="@MenuItemModal" OnValidSubmit="@SaveAddNewLinkData">
                        <DataAnnotationsValidator />
                        <ValidationSummary />
                        <div class="card-body">
                            <div class="row">
                                <div class="col-12">
                                    @*<div class="form-group">
            <label for="inputRole">Menu Item Category <span class="text-danger"></span></label>
            <InputSelect id="" class="form-control" ValueExpression="@(() => MenuItemModal.MenuItemGrantParentId)" Value="@MenuItemModal.MenuItemGrantParentId" ValueChanged="@((int? value) => MenuitemParentlistFill(value))" tabindex="3">
                <option value="0">-- Select Menu Item Category --</option>
                @foreach (var state in GrantParentList)
                {
                    <option value="@state.Value">@state.Text</option>
                }
            </InputSelect>
        </div>
        <div class="form-group">
            <label for="inputRole">MenuItem Sub Category <span class="text-danger"></span></label>
            <InputSelect id="" class="form-control" ValueExpression="@(() => MenuItemModal.MenuItemParentID)" Value="@MenuItemModal.MenuItemParentID" ValueChanged="@((int? value) => MenuitemChildListlistFill(value))" tabindex="3">
                <option value="0">-- Select Menu Item Sub Category --</option>
                @foreach (var state in ParentList)
                {
                    <option value="@state.Value">@state.Text</option>
                }
            </InputSelect>
        </div>
        <div class="form-group">
            <label for="inputRole">MenuItem Sub Category <span class="text-danger"></span></label>
            <InputSelect id="ShareHolderDirector" class="form-control" @bind-Value="MenuItemModal.MenuItemChildId" tabindex="1">
                <option value="0">-- Select Menu Item Sub Category --</option>
                @foreach (var state in ChildList)
                    {
                    <option value="@state.Value">@state.Text</option>
                    }
            </InputSelect>
        </div>*@

                                    <div class="form-group">
                                        <label>Menu Name: <span class="text-danger">*</span></label>
                                        <InputText id="txtfirstname" class="form-control" @bind-Value="MenuItemModal.MenuName" Placeholder="Enter Menu Name" tabindex="1" />
                                    </div>
                                    <div class="form-group">
                                        <label>Action Link: <span class="text-danger">*</span></label>
                                        <InputText id="txtEmail" class="form-control" @bind-Value="MenuItemModal.ActionLink" Placeholder="Enter Action Link Name" tabindex="3" />
                                    </div>
                                    <div class="form-group">
                                        <label>Icon: <span class="text-danger">*</span></label>
                                        <InputText id="txtEmail" class="form-control" @bind-Value="MenuItemModal.Icons" Placeholder="Enter Icon" tabindex="3" />
                                    </div>
                                    <div class="form-group">
                                        <label for="inputRole">MenuItem Type <span class="text-danger"></span></label>
                                        <InputSelect id="ShareHolderDirector" class="form-control" @bind-Value="MenuItemModal.MenuItemType" tabindex="4">
                                            <option value="">-- Select Menu Item Type --</option>
                                            <option value="Menu">Menu</option>
                                            <option value="Permission">Permission</option>
                                        </InputSelect>
                                    </div>

                                </div>

                            </div>
                        </div>
                        <div class="ml-lg-4 ml-md-4 ml-sm-4 mr-3">
                            <button type="submit" class=" ml-2 btn  float-right btn-primary" tabindex="9">Save</button>
                            <button type="reset" class="btn btn-secondary float-right" @onclick="CloseSideBar" tabindex="10">Cancel</button>
                        </div>
                    </EditForm>
                </div>
            </div>
            <div class="offcanvas-footer" kt-hidden-height="112" style="">
            </div>

            @*<img style="position:fixed;top:50%;left:70%;" src="assets/media/loading.gif" />*@

        </div>
    </div>
}
<FgcToast TostModels="@TostModelclass"> </FgcToast>
<ModalShowpopup showModal="@responseDialogVisibility" Message="@responseBody" title="@responseHeader" OnVisibilityChangedModel="@OnVisibilityChangedModel"></ModalShowpopup>

